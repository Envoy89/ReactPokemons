import React from 'react';

import s from './Footer.module.scss';

const Footer = () => (
  <footer className={s.root}>
    <div className={s.make}>Make with ❤️</div>
    <div className={s.our}>@Ruzil</div>
  </footer>
);

export default Footer;
